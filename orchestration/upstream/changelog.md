# 오케스트레이션 변경 로그

**시간순 요약 인덱스** — 규칙·에이전트·루프 변경 시 1~2줄로 기록한다.
**상세 추적**은 아래 두 로그를 통해 Task-ID로 연결:
- [WHAT: update-record.md](update-record.md) — 변경 파일·Exception
- [WHY: rationale-history.md](rationale-history.md) — 배경·대안·결정 (판단 섞인 변경만)

| 날짜 | 변경 | 이유 |
|------|------|------|
| 2026-04-11 | 카탈로그 + 분산 상세 재구성 | 428줄 단일 파일 → 카탈로그 ~70줄 + 상세 6개 파일. 진입 장벽 감소 |
| 2026-04-11 | 정책 번호 재정렬 (1~21) | 추가 순서로 꼬인 번호(9a→9→10→12→14→11→15…)를 논리적 그룹별 순차로 정리 |
| 2026-04-11 | `parse_marker()` BSD grep 버그픽스 — `grep -oEm1` → `grep -oEm1 -e` | macOS BSD grep이 `---MARKER:...` 패턴을 long option으로 오인 → 1차 마커 검출 실패 → 2차 fallback에서 본문 속 "FAIL" 단어 오탐. `-e` 플래그로 패턴 명시 |
| 2026-04-11 | `impl.sh` PLAN_VALIDATION_PASS 후 직접 dispatch | 기존: exit 0 → 플래그 재진입 → engineer. 플래그 영속성 버그로 재진입 실패 반복. 변경: validation PASS → engineer 루프 직접 진입 (재호출 불필요) |
| 2026-04-11 | `harness-review.py` EXPECTED_SEQUENCE에 `simple` 키 추가 | depth=simple에서 test-engineer/validator 스킵은 정상인데 MISSING_PHASE로 오탐 |
| 2026-04-15 | UI 디자인 게이트: 키워드 스캔 → opt-in frontmatter(`design: required`) 전환 | impl 텍스트에 "컴포넌트/디자인" 등 포함 시 무조건 UI_DESIGN_REQUIRED 트리거 → dead code 삭제 같은 비시각적 작업도 차단. "스크린샷이 달라지는가?"는 단어로 판단 불가하므로 명시적 opt-in으로 전환 |
| 2026-04-15 | `agent-boundaries.md`에 인프라 파일 접근 금지 규칙 추가 | engineer, pr-reviewer의 WASTE_INFRA_READ 반복 — `.claude/harness-*` 경로 Read/Glob 금지 명시 |
| 2026-04-15 | 모호성 정량화 (product-plan 스킬 + CLARITY_INSUFFICIENT 마커) | OMC deep-interview 참고. 5차원 모호성 점수 + 라운드 기반 인터뷰 + product-planner 에스컬레이션 |
| 2026-04-15 | Handoff 문서 패턴 (generate_handoff + write_handoff + explore_instruction 확장) | OMC handoff 참고. 에이전트 간 구조화된 인수인계 자동 생성. 에이전트 프롬프트 변경 없음 |
| 2026-04-15 | HUD Statusline (HUD 클래스 + hud.json + harness-monitor 전용 세션) | 진행 바 시각화 + 별도 세션 실시간 모니터링. Osmani 기사 참고 |
| 2026-04-15 | POLISH 모드 (@MODE:ENGINEER:POLISH + regression revert) | pr-reviewer LGTM 후 NICE TO HAVE 경량 정리. regression 실패 시 revert. OMC ai-slop-cleaner 참고 |
| 2026-04-15 | Circuit Breaker (시간 윈도우 120초 내 동일 fail_type 2회 → 조기 에스컬레이션) | harness_framework 참고. 기존 max 3 attempts와 독립 동작 |
| 2026-04-15 | `impl_loop.py` fallback import 블록에 `HUD`, `generate_handoff`, `write_handoff` 누락 수정 | `except ImportError` 경로에서 HUD 미import → `NameError: name 'HUD' is not defined` 크래시. try 블록과 동기화 |
| 2026-04-15 | `core.py` HUD.cleanup() 삭제 대신 완료 상태 기록으로 변경 | cleanup()이 HARNESS_DONE 직전에 hud.json 삭제 → harness-monitor가 최종 상태를 못 읽음. 삭제 대신 `status: "done"` 기록 후 파일 유지 |
| 2026-04-15 | Second Reviewer v1 → v3 전환 — 파일별 분할 + providers.py 어댑터 | v1(전체 diff stdin, 타임아웃)을 제거. v3: 파일별 patch + threading 병렬 + 2단계 프롬프트(NEED_FULL_FILE). providers.py에 BaseProvider/GeminiProvider 어댑터 패턴 |
| 2026-04-15 | Handoff 전 파이프라인 확장 — architect→validator→engineer→pr-reviewer | 기존 handoff는 engineer→test-engineer, SPEC_GAP만 커버. simple 모드 전체 체인에 handoff 추가. JSONL에 handoff 이벤트 로깅. harness-review.py가 handoff 유무에 따라 WASTE_DUPLICATE_READ 심각도/fix 분기 |
| 2026-04-15 | HUD 전체 라이프사이클 커버 — run_impl() 진입 시 생성 | 기존: run_simple/run_std 내부에서만 HUD 생성 → architect/plan-validation 사각지대. 변경: run_impl()에서 depth="auto"로 HUD 생성, preamble(architect+plan-validation) 포함, set_depth()로 depth별 에이전트 확장. run_simple/run_std/run_deep에 hud 파라미터 전달 |
| 2026-04-15 | `core.py` agent_call() active 플래그 경로 `/tmp/` → `state_dir`로 수정 | 훅(agent-boundary/issue-gate)은 `.claude/harness-state/`에서 탐색, agent_call은 `/tmp/`에 생성 → 경로 불일치로 에이전트를 메인 Claude로 오판 → src/** Edit·이슈 생성 차단 |
| 2026-04-15 | `harness-review.py` INFRA_EXCLUSIONS에 `handoff` 추가 | handoff 파일은 에이전트 간 인수인계 문서로 의도된 Read인데 `.claude/` 경로 포함으로 WASTE_INFRA_READ 오탐 |
| 2026-04-15 | HUD `_write_json` 진단 강화 + fallback 경로 | `_hud_path`가 None일 때 cwd 기반 fallback 추론, `except OSError: pass` → 에러 메시지 출력, 첫 agent_start 시 one-time 진단 로그 |
| 2026-04-15 | `harness-router.py` 대폭 간소화 — 7카테고리 → 3카테고리(BUG/UI/IMPL) | Haiku 폴백 90% 타임아웃, Adaptive Interview 0.2% 성공률, GREETING/QUESTION/GENERIC/AMBIGUOUS 전부 동일 동작(sys.exit). Haiku·Interview·extract_intent·socrates 에이전트 전부 제거. 스킬(/qa /ux /product-plan)이 정밀 라우팅 담당 |
| 2026-04-15 | QA 에이전트에 `update_issue`+`add_issue_comment` 도구 추가 + `issue-gate.py`에 update_issue 차단 추가 | 메인 Claude가 이슈 수정(edit)도 직접 하면 안 됨. QA/designer 에이전트만 이슈 생성+수정 가능. #106에서 메인이 직접 edit해서 요구사항 왜곡 3회 발생 |
| 2026-04-15 | QA 이슈 본문에 유저 원문 인용 필수 | #106에서 architect가 QA 요약을 과잉 해석하여 3회 재작업. 유저 원문이 이슈 → impl → engineer까지 전달되면 architect의 자체 해석 방지 |
| 2026-04-15 | `plan_loop.py` product-planner 타임아웃 300s → 600s | MCP 문서 검색(apps-in-toss)에 시간 소모되어 PRD 작성 전 타임아웃. 코인 시스템 기획에서 발생 |
| 2026-04-15 | Marker Safety 규칙 추가 — UNKNOWN 마커 시 진행 금지 | product-planner가 마커 없이 Q1/Q2 질문 → UNKNOWN → plan_loop가 architect까지 폭주. 전체 parse_marker 감사 후 진행 게이트 4곳 수정: plan_loop(pp/arch_sd/arch_mp) + impl_router(arch). orchestration-rules.md에 Marker Safety 원칙 추가, product-planner.md에 마커 필수 경고 추가 |
| 2026-04-15 | Plan 루프 HUD 추가 + 로그 보강 | plan_loop.py에 HUD 없어서 진행 상태 파악 불가. HUD depth="plan" 추가 (agents: product-planner→architect-sd→design-validation→architect-mp→plan-validation). 각 단계 마커 결과를 `[HARNESS] agent → MARKER` 형식으로 출력 |
| 2026-04-15 | drift-check에 스크립트→orchestration 상세 문서 역매핑 추가 | plan_loop.py 변경 시 orchestration/plan.md 동기화 누락 방지 |
| 2026-04-15 | plan_loop architect에 pp_out 전문 대신 prd.md 경로만 전달 | architect가 pp_out(수만 토큰)을 prd.md로 다시 쓰려고 Bash heredoc 루프에 빠져 900초 타임아웃. 경로만 넘기고 Read하게 변경 |
| 2026-04-15 | plan 루프 전면 타임아웃 조정 + 도구 차단 | Bash 20분 < plan loop 50분(worst case) 문제. product-plan 스킬에 timeout 3600000(60분) 명시. architect-sd/mp 900s→600s. agent_call에 에이전트별 disallowedTools 추가 (product-planner: Bash 차단, validator/pr-reviewer/critic: Bash+Write+Edit 차단) |
| 2026-04-16 | plan_loop architect-mp: design_doc 오탐 수정 + module 파라미터 + TASK_DECOMPOSE 분기 | 1) design_doc regex가 docs/sdk.md를 먼저 매칭 → architecture*.md 우선 매칭으로 변경. 2) MODULE_PLAN에 module 파라미터 누락 → stories.md에서 추출. 3) stories.md impl 3개 이상이면 TASK_DECOMPOSE로 자동 분기 |
| 2026-04-16 | 하네스 26개 항목 일괄 개선 (Ralph 10회) | FIX 9건: plan 체크포인트(prd/architecture 스킵), plan 메타데이터 저장, std/deep pr-reviewer handoff, test-engineer handoff 경로, SPEC_GAP 마커 기반 추출, validator handoff, kill_check rework, watchdog stdout.close, merge checkout 체크. SKIP 14건(의도적/오탐). DEFER 4건. |
| 2026-04-16 | PR 워크플로우 전면 재구성: 매 커밋 push + PR 자동 생성 + squash merge | push_and_ensure_pr() 신규, merge_to_main() --squash 전환, impl_loop 7곳에 push 추가 |
| 2026-04-16 | 브랜치 전략: 로컬 merge → GitHub PR 경유로 전환 | feature branch를 remote에 push → gh pr create → gh pr merge. branch 이력이 GitHub에 남고 PR로 변경 추적 가능. merge_to_main() 전면 재작성. simple depth도 LGTM 게이트에 포함 |
| 2026-04-16 | autocheck_fail 피드백 + lint scope 필터 | 1) simple depth: autocheck_fail 시 에러 내용(autocheck.log)을 engineer task에 포함 (std/deep는 이미 있었음). 2) automated_checks lint: 변경된 src 파일만 lint 실행 → pre-existing 에러로 인한 오탐 방지 |
| 2026-04-16 | build_command 게이트 추가 | harness.config.json에 build_command 필드 추가. automated_checks check #6 (lint→build→commit 순서) + POLISH regression (lint→build→test). 빌드 실패 코드가 merge되는 사고 방지. config.py, helpers.py, impl_loop.py 수정 |
| 2026-04-17 | TDD 게이트 Phase 1: std/deep에 TDD 순서 도입 | test-engineer를 engineer 앞으로 이동. 기존 TEST 모드 제거, TDD 모드 단일. attempt 0만 test-engineer 호출. engineer 자체 vitest 지침 추가. simple 영향 없음. |
| 2026-04-17 | Phase 2 감사 수정: product-plan.md 6단계 executor.py 오호출 -> Agent 도구 병렬 호출로 재작성, orchestration-rules.md 데이터 전달 규칙 루프 귀속 수정 (plan -> 설계) | 감사에서 CRITICAL 2건 발견. 엣지케이스: LIGHT_PLAN에 design-handoff 미전달은 현재 문제 없음 (버그 경로라 design-handoff가 없는 게 정상) |
| 2026-04-17 | 디자인 게이트 Phase 1: 3개 루프 분리 + ux-architect 에이전트 신설 | 기획 루프에 UX 단계가 없어 engineer가 디자인 추측 구현하는 빈 구간 해소. 기획-UX(plan.md) / 설계(system-design.md) / 구현(impl.md) / 디자인(design.md) 4개 문서 분리. ux-architect(UX_FLOW/UX_SYNC 2모드), validator UX_VALIDATION 모드, designer plan loop 파라미터(skip_issue_creation, save_handoff_to) 추가. |
| 2026-04-18 | ux-architect UX_REFINE 모드 추가 | 기존 Pencil 디자인의 레이아웃·비주얼을 개편하는 3번째 모드. 기능/플로우 변경 없이 화면 단위 리디자인. ux 스킬(REFINE 감지) → ux-architect(Pencil 읽기 → 와이어프레임 + 리디자인 노트) → 유저 승인 → designer SCREEN 모드. Pencil MCP 읽기 도구(batch_get, get_screenshot, get_variables, get_editor_state) 부여. core.py Marker enum에 UX_REFINE_READY 추가. |
| 2026-04-16 | 실전 10회 로그 기반 버그 6건 수정 | A) POLISH revert `git reset --hard` → 선택적 파일 복원으로 merge conflict 해소. B) automated_checks no_changes → `git diff main..HEAD` 커밋 변경 감지 추가. D) architect module-plan TS 타입 체크리스트 추가. E) automated_checks에 lint_command 실행 추가. F) pr-reviewer 240s→360s. C) DEFER(에이전트 판단). WASTE_INFRA_READ: handoff 추가로 자연 해소. |
| 2026-04-19 | Phase 3 세션 격리 — `hooks/session_state.py` 단일 상태 API 도입 | `.claude/harness-state/`를 `.sessions/{sid}/`, `.issues/{prefix}_{issue}/`, `.global.json` 으로 재구성. 훅이 `ss.active_agent(stdin_data)` → `live.json.agent` 단일 소스로 판정. **env var 폴백 / 15분 TTL / 화이트리스트 필터 제거**. SessionStart 훅이 `.session-id` 기록 + 레거시 `.flags/` 1회 마이그레이션 + stale 세션(6h) / 이슈 lock(30m) cleanup. 이슈 lock은 세션 밖 경로(`.issues/`)에 기록되어 두 세션이 같은 이슈 동시 진입 차단. atomic write(O_EXCL + fsync + rename) + `_meta` envelope으로 소유자 검증. PR #24/#26/#29 재현 박제 테스트 포함 — 150 tests pass. |
| 2026-04-19 | Phase 3 후속 — executor.heartbeat_loop 이슈 lock heartbeat 갱신 누락 수정 | 초기 구현에서 `executor.py`의 15초 heartbeat 루프가 legacy lease 파일만 갱신하고 Phase 3 이슈 lock(`.issues/{prefix}_{issue}/lock`)은 claim 시점 이후 방치 → 30분 이상 실행되는 deep impl 루프가 stale 판정으로 다른 세션에게 탈취되는 버그. `ss.heartbeat_issue_lock` 호출 추가 + 회귀 테스트(`test_heartbeat_refresh_prevents_stale_takeover`). 총 154 tests pass. phase3-session-isolation.md §D5(multi-session `.session-id` race known limitation), §D6(heartbeat 갱신 요구사항) 문서화. |
| 2026-04-19 | orch-rules-first 훅 경고형 전환 + orchestration-rules.md 정제 | 훅이 `deny` → `additionalContext`로 바뀌어 정당한 리팩터·버그픽스도 허용하되 규칙 선행 수정을 권장으로만 안내. orchestration-rules.md에서 구현 디테일 18개 섹션(plan/agent timeout 수치, GitHub PR 워크플로우, watchdog, budget_check 예외, POLISH revert 세부, automated_checks 탐지, HUD Statusline, Circuit Breaker, Second Reviewer v3, Autocheck 피드백 세부, Lint/Build Scope 필터, Build Gate 세부, Phase 3 세션 격리 상세 등)을 제거하거나 1~2줄로 축약. 세션 격리 상세는 `docs/session-isolation/phase3-session-isolation.md` 링크로 대체. 418줄 → 229줄. 규칙 문서가 "단일 룰 소스" 역할을 회복. |
| 2026-04-19 | simple depth 회귀 차단 — `run_automated_checks(run_tests=True)` + DOM/텍스트 변경 std 승격 | mb #129(ResultPage 리디자인) 머지 후 vitest 5건 회귀 발견. 원인: (a) depth=simple에서 test-engineer/TDD 선행이 스킵돼 회귀 테스트가 실행되지 않음 (b) DOM/텍스트 변경(이모지→SVG)이 simple로 잘못 분류됨. 수정: `helpers.run_automated_checks`에 `run_tests=False` 파라미터 추가 + `impl_loop.run_simple`이 True로 호출(std/deep는 GREEN에서 별도 실행하므로 False 유지 — 중복 회피). 분류 룰: `impl_router.ensure_depth_frontmatter` + LIGHT_PLAN 프롬프트 + `agents/architect.md`에 "__tests__ assertion 대상(DOM·텍스트·testid) 변경은 std 승격" 명시. 회귀 테스트 4건 추가(`TestRunAutomatedChecksTestRegression`). 총 112 tests pass. |
| 2026-04-21 | 외부 알림 (`harness/notify.py` + `write_run_end` 통합) | OMC/Devin 대비 구멍이던 세션 밖 알림 채널 신설. `HARNESS_NOTIFY` env var로 opt-in: `osascript`(macOS Dock) / `file:PATH` / `webhook:URL` 혹은 쉼표 조합. 트리거는 `HARNESS_DONE` + `*_ESCALATE/ESCALATION_NEEDED/CONFLICT/CRASH`만 (중간 마커 스팸 방지). `RunLogger.write_run_end`에서 JSONL `cost_usd` 합산 후 `notify()` 호출. 기본 OFF(env 미설정 시 0-cost no-op). 알림 실패는 하네스 흐름 영향 없게 try/except. |
| 2026-04-21 | agent-gate.py Mode-level 게이트 — architect/validator 세분화 | mb #158에서 메인 Claude가 architect Mode B(MODULE_PLAN) + validator Plan Validation을 Agent 도구로 직접 호출해 plan_loop를 우회. `HARNESS_ONLY_AGENTS=("engineer",)`만 차단하던 구조 → Mode 단위 게이트로 확장: `ARCHITECT_HARNESS_ONLY_MODES={MODULE_PLAN, SPEC_GAP}`, `VALIDATOR_HARNESS_ONLY_MODES={PLAN_VALIDATION, CODE_VALIDATION, BUGFIX_VALIDATION}`. `detect_architect_mode()`/`detect_validator_mode()`가 프롬프트 파싱해 Mode 식별 후 HARNESS_ACTIVE 플래그 없으면 deny. SYSTEM_DESIGN(A)/TASK_DECOMPOSE(D)/TECH_EPIC(E)/LIGHT_PLAN(F)/DESIGN_VALIDATION은 product-plan 스킬 6단계처럼 메인 Claude 직접 호출 허용. policies.md §2 재작성. 회귀 테스트 8건(`TestAgentGateModeLevel`). |
| 2026-04-21 | 재시도 generic 분기에 이전 실패 로그 자동 인라인 (`_extract_generic_fail_hint`) | `impl_loop.py`의 simple·std/deep else 분기는 `explore_instruction`만 주고 직전 실패 로그 경로만 알려주는 구조였는데 engineer가 Read를 생략하고 같은 실수를 반복하는 버그 반복 관찰 (pr_fail은 2026-04-19 PR #45에서 인라인 승격됨 — 같은 원리). 헬퍼는 `prev_dir/*.log` 중 최신 파일 마지막 2KB를 code fence로 인라인. task_map에 매핑되지 않은 fail_type(미래 확장 포함)을 일괄 커버. simple 모드 generic + std/deep `task_map.get` default 2곳 적용. 5 unit test 통과. |
| 2026-04-24 | plan-reviewer 에이전트 신설 — 기획 판단 게이트(기획팀장 포지션) | validator(UX)는 형식 체크리스트(화면 커버리지, 상태 정의, 수용 기준)만 검사하여 PRD/UX Flow의 **현실성·MVP 과적재·UX 저니 어색함·숨은 가정**이 통과되는 구멍이 있었다. plan-reviewer(ReadOnly)를 추가해 5개 차원(현실성·MVP 균형·제약 정합·UX 저니·숨은 가정)을 판단 레벨에서 리뷰. `plan_loop.py`에서 validator(UX) PASS 이후(또는 UX_SKIP 시 PRD 직후) 호출. `PLAN_REVIEW_PASS`→유저 승인 ① / `PLAN_REVIEW_CHANGES_REQUESTED`→메인 Claude 리포트 원문 전달 → 유저 3지선다(수정 반영/override/취소). override는 `{prefix}_plan_review_override` 1회성 플래그로 재호출 시 리뷰 스킵. `core.py _AGENT_DISALLOWED`·`orchestration/plan.md`·`orchestration/agent-boundaries.md`·`orchestration-rules.md`·`commands/product-plan.md` 동기화. |
| 2026-04-24 | plan-reviewer 전문성 확장 — 경쟁 분석 + 과금 설계 + 기술 실현성 3개 스페셜리스트 추가 (5→8 차원) | 기획팀장 일반 판단만으로는 시장 진입 실패 패턴·BM 구조적 리스크·플랫폼 SDK 제약이 걸러지지 않음. 페르소나에 ② 경쟁/시장 분석가, ③ 과금/수익화 설계, ④ 기술 실현성 판단자 3개 전문성 추가. 차원 6(경쟁 맥락) / 7(과금 설계) / 8(기술 실현성) 신설. 기술 실현성은 **외부 기술 사실만** 참조(`docs/sdk.md`·`docs/reference.md`·`docs/architecture.md`) — `trd.md`는 architect 내부 결정이라 reviewer가 역방향으로 기획을 쳐내는 오염을 막기 위해 금지(planner와 동일 사유). `hooks/agent-boundary.py` READ_DENY_MATRIX에 `plan-reviewer` 엔트리 추가(src/**, docs/impl/**, trd.md). `orchestration/agent-boundaries.md`·`orchestration-rules.md` 동기화. |
| 2026-04-25 | plan-reviewer 위치 변경 — ux-architect **뒤**에서 **앞**으로 이동 | jajang 실전에서 reviewer가 UX Flow까지 나온 다음 FAIL하면 "고칠 게 너무 많다" 문제. PRD 단계 문제(현실성·MVP·경쟁·BM·기술 실현성 6개 차원)를 UX Flow 생성 전에 먼저 거르면 ux-architect 재작업 비용이 없다. UX 저니 차원(4번)은 PRD의 "화면 인벤토리 + 대략적 플로우"로 고수준 판정만 하고, 상세 UX 형식 체크는 validator(UX)가 전담. 새 순서: `planner → plan-reviewer → UI check → ux-architect → validator(UX) → 유저 승인 ①`. Task-ID: HARNESS-CHG-20260425-01. |
| 2026-04-25 | 거버넌스 프레임워크 — Task-ID + WHAT/WHY 로그 + 경로 기반 drift-check | changelog 한 곳에 WHAT/WHY가 섞여 장기 추적성 약함. 친구 TDM 프로젝트 거버넌스(SSOT + Task-ID + 두 로그 + diff-scoped exception) 벤치마크. 도입: (1) `orchestration/update-record.md` (WHAT — 변경 파일·Exception), (2) `orchestration/rationale-history.md` (WHY — 판단 섞인 변경만), (3) `hooks/harness-drift-check.py` 경로 정규식 PATH_RULES로 재작성 + `Document-Exception:` diff 추가 라인만 파싱(과거 누적 예외 무효). 3중 pre-commit 강제는 도입하지 않음(개발 인프라에 과잉). 이 엔트리부터 Task-ID 부여 시작. Task-ID: HARNESS-CHG-20260425-02. |
| 2026-04-26 | setup-harness.sh — GitHub MCP 등록 안내 추가 | jajang에서 ISSUE_CREATORS 에이전트의 mcp__github__create_issue가 silently 실패. MCP 서버 미등록이 root cause. 새 머신 setup 시 동일 데드락 방지 위해 setup-harness.sh가 `claude mcp list`로 자동 체크 + 미등록 시 안내(자동 등록은 토큰 보안상 안 함). orchestration-rules.md ISSUE_SYNC 섹션에 사전 요건 명시. Task-ID: HARNESS-CHG-20260426-01. |
| 2026-04-26 | 듀얼 모드 — 디자인 토큰 우선 가드레일 (UI 컴포넌트 갈아엎기 방지) | jajang에서 ux-flow.md만 있고 Pencil 시안 미도착 상태로 구현 시작됨. 단순 듀얼은 시안 도착 시 색·폰트·레이아웃 직접 박힌 컴포넌트 전체 재작업 폭발. 가드레일 도입: (1) architect TASK_DECOMPOSE 첫 impl을 `01-theme-tokens.md`로 강제 (`src/theme/{colors,typography,spacing,index}.ts`) (2) MODULE_PLAN UI impl에 theme 의존성 + 리터럴 금지 수용 기준 (3) engineer가 hex/rem/font-name 직접 박기 금지(`grep -rE "#[0-9a-fA-F]{6}|fontFamily.*'[A-Z]" src/` 0건). 판정 조건: ux-flow.md §0 디자인 가이드 존재 + design-handoff.md 미존재. Task-ID: HARNESS-CHG-20260426-02. |
| 2026-04-26 | ux-architect Anti-AI-Smell 확장 — "다크 네이비+골드 단톤" 금지 + 라이트/다크 모드 둘 다 강제 | jajang ux-architect 산출물이 "딥 미드나잇 네이비 + 골드/앰버 엑센트" Claude/AI 클리셰 톤으로 나옴(유저 reject). 슬립·명상·오디오 류에서 자동 발현되는 판박이 패턴이라 일반화 차단. Step 5 디자인 가이드 컬러 팔레트 표를 Day/Night 두 컬럼으로 강제(한 모드만 정의 금지). 두 모드 무드 일관·WCAG AA 4.5:1·다크는 단순 반전 아닌 톤다운 가이드라인 추가. Task-ID: HARNESS-CHG-20260426-04. |
| 2026-04-26 | ux-architect Anti-AI-Smell 강화 — 색 swap 우회 차단 (구조 패턴 + 자기 정당화 + 라이트 우선) | HARNESS-CHG-20260426-04에서 박은 "다크+골드 금지"를 ux-architect가 골드→민트로 swap만 하고 동일 구조 반환. 색 금지 룰은 무한 추가될 거고 매번 우회됨이 명확. 진짜 클리셰는 구조: 다크 단색+단일 엑센트+outline 카드+플랫 글리프=Spotify-like. 5가지 구조 자가 점검 (3개 이상 reject) + 경쟁 앱 3개 + 구조적 차별점 자기 정당화 블록 필수 + 라이트 우선 사고 + 표현 매체 1개 이상 채택. 슬립/명상/오디오는 라이트 베이스 + 텍스처/일러스트 정체성 권장. Task-ID: HARNESS-CHG-20260426-05. |
| 2026-04-27 | 워크트리 격리 setup 기본 활성화 | `setup-harness.sh`가 신규 `harness.config.json` 생성 시 `"isolation": "worktree"` 기본 포함. `.gitignore`에 `.worktrees/` 자동 등록 (git repo). 기존 파일은 자동 추가하지 않고 안내만 (유저 의도 존중). config.py default는 건드리지 않음 — 기존 프로젝트 영향 없음. Task-ID: HARNESS-CHG-20260427-01. |
